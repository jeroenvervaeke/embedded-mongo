[package]
name = "embedded-mongodb-wire"
version = "0.1.0"
edition = "2024"
description = "OP_MSG parsing shared by the embedded-mongodb driver bindings"
license = "SSPL-1.0"
publish = false

[dependencies]
bson = "3.1.0"
